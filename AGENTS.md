# HueBeatController 작업 규칙

## Git 커밋 컨벤션

- 커밋 제목은 Conventional Commits 형식을 사용한다: `<type>: <한글 중심 작업 요약>`
- 기능 영역이 분명하면 `<type>(<scope>): <한글 중심 작업 요약>` 형식도 사용할 수 있다.
- 작업 요약과 커밋 본문은 한글을 기본으로 쓰되, Hue Entertainment, API, UI처럼 제품명이나 기술 용어는 영문을 그대로 사용한다.
- 제목은 명령형으로 간결하게 작성하고 마침표를 붙이지 않는다.

사용 가능한 type:

- `feat`: 사용자 기능 추가 또는 동작 변경
- `fix`: 버그 수정
- `refactor`: 기능 변화가 없는 코드 구조 개선
- `style`: UI/CSS 또는 코드 포맷 변경
- `docs`: 문서 변경
- `test`: 테스트 추가·수정
- `build`: 빌드 및 의존성 변경
- `chore`: 기타 유지보수

예시:

- `feat(entertainment): 음악 구간별 미디어아트 연출 추가`
- `fix(bridge): 전구 그룹 명령 누락 문제 수정`
- `style(ui): 음악 제어 화면 가독성 개선`
- `docs: 실행 및 Bridge 연결 방법 보완`

변경 사항이 여러 개라 설명이 필요하면 커밋 본문에 한글 bullet로 핵심 작업과 검증 결과를 적는다.

## 작업 및 검증 원칙

- 인증키, Bridge 로컬 설정, 음원과 분석 데이터는 Git에 커밋하지 않는다.
- 변경 후 `dotnet build`와 수정한 JavaScript의 구문 검사를 실행한다.
- Entertainment 연출 변경 시 A/B 그룹의 전구 수를 같게 유지하고 같은 배열 인덱스를 좌우 한 쌍으로 매핑한다. A/B 논리 그룹과 실제 Bridge 소속은 분리하며, 프레임 전송 직전에 전구 소속 Bridge로 자동 분배한다. 각 Bridge의 Entertainment 영역 10개 제한을 넘지 않는다.
- 현장 배치의 `Bridge 1 → A / Bridge 2 → B` 자동 배치는 8+8 초기 설정을 위한 편의 기능이다. 자동 배치 이후 사용자가 음악 그룹에서 Bridge 소속과 무관하게 순서나 구성을 다시 바꿀 수 있어야 한다.
- 기존 사용자 설정과 관계없는 파일은 임의로 수정하거나 삭제하지 않는다.
