# HueBeatController 작업 규칙

## 공식 제품 방향

- 모든 구현 판단의 기준 문서는 `docs/OFFLINE_SHOW_IMPLEMENTATION_PLAN.md`다. 작업 시작 전에 관련 단계를 확인하고, 방향이나 우선순위가 바뀌면 코드보다 먼저 이 문서를 수정한다.
- 최종 구조는 `음원 사전 분석 → 자동 조명 악보 초안 → 모의 전구 검토·수정 → 확정 악보 → 2대 Bridge Entertainment 출력`이다.
- LedFx 실시간 효과, 재생 중 즉흥 임계값 판정, 샘플 한 곡의 특정 시간에 맞춘 규칙은 최종 제품 경로로 사용하지 않는다.
- Beat, Downbeat, Onset, 구간 후보와 조명 Cue를 구분한다. 분석 결과는 후보이며 실제 출력 명령은 편집·확정한 조명 악보에서 생성한다.
- 음악 분석 revision, 자동 악보 초안, 사용자 확정 악보, 장비 설정을 분리한다. 재분석으로 사용자 악보를 덮어쓰지 않는다.
- 웹 모의 전구와 실제 Hue는 같은 확정 악보와 프레임 계산기를 사용한다. 장비 지연 보정은 출력 단계에서만 적용한다.
- 구현 순서는 분석 검증, 악보 스키마, 자동 초안, 파형 편집기, 결정적 재생, 2대 Bridge 현장 보정, 배포 순서로 진행한다.
- 새 기능 완료 시 공식 계획 문서의 현재 상태와 다음 작업도 함께 갱신한다.

## Git 커밋 컨벤션

- 커밋 제목은 Conventional Commits 형식을 사용한다: `<type>: <한글 중심 작업 요약>`
- 기능 영역이 분명하면 `<type>(<scope>): <한글 중심 작업 요약>` 형식도 사용할 수 있다.
- 작업 요약과 커밋 본문은 한글을 기본으로 쓰되, Hue Entertainment, API, UI처럼 제품명이나 기술 용어는 영문을 그대로 사용한다.
- 제목은 명령형으로 간결하게 작성하고 마침표를 붙이지 않는다.

사용 가능한 type:

- `feat`: 사용자 기능 추가 또는 동작 변경
- `fix`: 버그 수정
- `refactor`: 기능 변화가 없는 코드 구조 개선
- `style`: UI/CSS 또는 코드 포맷 변경
- `docs`: 문서 변경
- `test`: 테스트 추가·수정
- `build`: 빌드 및 의존성 변경
- `chore`: 기타 유지보수

예시:

- `feat(entertainment): 음악 구간별 미디어아트 연출 추가`
- `fix(bridge): 전구 그룹 명령 누락 문제 수정`
- `style(ui): 음악 제어 화면 가독성 개선`
- `docs: 실행 및 Bridge 연결 방법 보완`

변경 사항이 여러 개라 설명이 필요하면 커밋 본문에 한글 bullet로 핵심 작업과 검증 결과를 적는다.

## 작업 및 검증 원칙

- 인증키, Bridge 로컬 설정, 음원과 분석 데이터는 Git에 커밋하지 않는다.
- 변경 후 `dotnet build`와 수정한 JavaScript의 구문 검사를 실행한다.
- Entertainment 연출 변경 시 A/B 양쪽에 전구가 있으면 전구 수를 같게 유지하고 같은 배열 인덱스를 좌우 한 쌍으로 매핑한다. 사전 분석 연출의 임시 현장 테스트는 한쪽만 있어도 허용하며 빈쪽은 소등한다. A/B 논리 그룹과 실제 Bridge 소속은 분리하며, 프레임 전송 직전에 전구 소속 Bridge로 자동 분배한다. 각 Bridge의 Entertainment 영역 10개 제한을 넘지 않는다.
- 현장 배치의 `Bridge 1 → A / Bridge 2 → B`는 음악 그룹의 전구 소속·순서에 대한 단일 기준이다. 음악 그룹에서 별도로 전구를 이동·정렬하지 않고 Bridge 배치에서 수정하면 자동 반영한다. 피아노 음계는 전구 ID에 저장해 이 정렬과 분리한다. Bridge 한쪽이 일시적으로 응답하지 않으면 그쪽의 저장된 배치를 지우지 않는다.
- 일반 그룹 UI는 사용하지 않는다. 일반 제어 화면에서는 Bridge별 전구 순서·배치 색상 확인과 전체/개별 제어를 제공하고, 음악 그룹은 Entertainment A/B만 유지한다.
- Bridge 간 일괄 이동은 선택 전구를 모두 사전 검증한 뒤 출발 Bridge에서 해제하고, 대상 Bridge 검색은 한 번만 실행한다. 전구별 이름과 현장 순서를 복원하고 부분 실패 결과를 사용자에게 구분해 표시한다.
- 기존 사용자 설정과 관계없는 파일은 임의로 수정하거나 삭제하지 않는다.
